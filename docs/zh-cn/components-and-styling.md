# 🧱 组件与样式

## 组件最佳实践

#### 尽可能把东西放在最接近使用处的位置

将组件、函数、样式、状态等尽可能靠近它们被使用的地方。这不仅能提升代码可读性与可理解性，也能改善性能（例如减少状态更新导致的冗余重渲染）。

#### 避免超大组件 + 在组件内部嵌套多层渲染函数

不要在组件内部堆叠多个渲染函数，这会很快失控。更好的做法是：如果某段 UI 可以被视为一个“单元”，就把它抽成独立组件。

```javascript
// this is very difficult to maintain as soon as the component starts growing
function Component() {
  function renderItems() {
    return <ul>...</ul>;
  }
  return <div>{renderItems()}</div>;
}

// extract it in a separate component
function Items() {
  return <ul>...</ul>;
}

function Component() {
  return (
    <div>
      <Items />
    </div>
  );
}
```

#### 保持一致

保持代码风格一致。例如，如果你用 PascalCase 来命名组件，就在全项目保持一致。大多数一致性依赖 lint 与格式化工具来保证，因此务必在项目里正确配置它们。

#### 限制组件对外暴露的 props 数量

如果一个组件接收的 props 太多，可以考虑拆分为多个组件，或使用组合（composition）方式，通过 children 或 slots 来组织。

[组合（Composition）示例代码](../apps/react-vite/src/components/ui/dialog/confirmation-dialog/confirmation-dialog.tsx)

#### 将共享组件抽象为组件库

在较大的项目中，为共享组件建立抽象层通常是个好主意：能让应用更一致、更易维护。在创建抽象之前先识别重复点，避免“错误抽象”。

[组件库示例代码](../apps/react-vite/src/components/ui/button/button.tsx)

同样建议对第三方组件进行封装，以便适配应用需求。这样未来调整底层实现时，更不容易影响到业务功能。

[第三方组件封装示例代码](../apps/react-vite/src/components/ui/link/link.tsx)

## 组件库

任何项目通常都需要一些通用 UI 组件，例如：弹窗、Tabs、侧边栏、菜单等。与其从零实现，不如优先考虑成熟、经过验证的组件库。

#### 全功能组件库（Fully featured）

这类组件库通常自带完整样式。

- [Chakra UI](https://chakra-ui.com/) - 体验很好的组件库，默认设计较为合理，原生支持无障碍能力，适合快速原型开发。

- [AntD](https://ant.design/) - 组件非常丰富，适合后台/管理端。缺点是要适配自定义设计时可能更难改样式。

- [MUI](https://mui.com/material-ui/) - React 生态最流行的组件库之一。既可遵循 Material Design，也可作为无样式（headless）组件使用。

- [Mantine](https://mantine.dev/) - 现代 React 组件库，组件与 hooks 丰富，可定制性强。

#### Headless 组件库

这类组件库提供无样式的组件。如果你需要落地特定设计系统，往往比把 Material UI 之类全功能组件库“硬改”更容易。可选项包括：

- [Radix UI](https://www.radix-ui.com/)
- [Headless UI](https://headlessui.dev/)
- [react-aria](https://react-spectrum.adobe.com/react-aria/)
- [Ark UI](https://ark-ui.com/)
- [Reakit](https://reakit.io/)

## 样式方案

为 React 应用添加样式有很多方式，以下是一些常见选择：

- [tailwind](https://tailwindcss.com/)
- [vanilla-extract](https://github.com/seek-oss/vanilla-extract)
- [Panda CSS](https://panda-css.com/)
- [CSS modules](https://github.com/css-modules/css-modules)
- [styled-components](https://styled-components.com/)
- [emotion](https://emotion.sh/docs/introduction)

注意：使用 React Server Components 时，需要考虑它们通常要求“零运行时（zero runtime）”样式方案。

随着 headless 组件库的流行，还出现了另一类方案：提供可直接拷贝进项目的组件代码（而不是以 npm 包形式安装），并自带样式方案，允许你按需定制。

- [ShadCN UI](https://ui.shadcn.com/)
- [Park UI](https://park-ui.com/)

## Storybook

[Storybook](https://storybook.js.org/) 是一个非常适合“隔离开发与测试组件”的工具，可以把它理解为项目组件的目录/画廊。对组件开发与可发现性（discoverability）很有帮助。

[Storybook Story 示例代码](../apps/react-vite/src/components/ui/button/button.stories.tsx)
